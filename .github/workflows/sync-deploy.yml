name: Sync & deploy

on:
  schedule:
    - cron: '17 * * * *' # hourly; off the top of the hour to avoid the scheduler rush
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm test

      - name: Sync GitHub Projects data
        env:
          PROJECTS_TOKEN: ${{ secrets.PROJECTS_TOKEN }}
        run: |
          if [ -n "$PROJECTS_TOKEN" ]; then
            npm run sync
          else
            echo "::warning title=PROJECTS_TOKEN not set::Deploying fixture data. Add the PROJECTS_TOKEN repository secret to sync real issues."
            npm run data:fixtures
          fi

      - run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
